.vscode/
.continue/
*.json.bak
backup_*.json
*.code-workspace

# Build outputs (regenerated by `npm run dist`)
/dist/
/distout/
/dist_new/
/release/
/electron/dist/
/electron/dist-build/
node_modules/

# Build-time / debug artifacts (recreated per build)
_build_staging_*/
_temp_*.js

# Residual old-release/debug folders (registry leftover; delete when unlocked)
/_out217/
/release_new/
/release_v2.0.17/
/extracted_pos201/
/_release_preview/
/_release_test/

# Local helper to remove the locked leftovers (double-click after reboot)
cleanup-locked-dirs.bat